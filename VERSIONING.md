# 🔖 Руководство по версионированию проекта

## Обзор системы версионирования

Проект **Monitoring Stack Automation** использует автоматическую систему версионирования, которая отслеживает версию проекта, git коммиты и отображает эту информацию в каждом запуске Jenkins pipeline.

---

## 📋 Компоненты системы

### 1. Файл VERSION

Файл `VERSION` в корне проекта содержит **базовую версию** проекта:

```
3.0.2
```

Это основной источник истины для версии проекта.

### 2. Скрипт get-version.sh

Скрипт `tools/get-version.sh` собирает полную информацию о версии:

- **Базовая версия** из файла VERSION
- **Git commit hash** (короткий и полный)
- **Git branch** (текущая ветка)
- **Build date** (дата и время сборки)
- **Build timestamp** (для уникальных ID сборок)
- **Uncommitted changes** (наличие несохраненных изменений)

#### Режимы работы

```bash
# Только версия
./tools/get-version.sh version
# Output: 3.0.2

# Короткая версия с коммитом
./tools/get-version.sh short
# Output: 3.0.0-a1b2c3d

# Полная версия (по умолчанию)
./tools/get-version.sh
# Output: 3.0.0-a1b2c3d (master, 2026-01-23 14:30:00 UTC)

# JSON формат
./tools/get-version.sh json
# Output: {"version":"3.0.0","git_commit":"a1b2c3d",...}

# Environment variables формат
./tools/get-version.sh env
# Output: VERSION_BASE=3.0.0\nVERSION_GIT_COMMIT=a1b2c3d\n...

# Красивый баннер для консоли
./tools/get-version.sh banner
# Output: ASCII banner с полной информацией
```

### 3. Интеграция с Jenkins Pipeline

Jenkins pipeline автоматически:

1. Вызывает `get-version.sh` на этапе **"CI: Информация о версии проекта"**
2. Отображает красивый баннер с версией в консоли
3. Сохраняет версионную информацию в переменные окружения:
   - `VERSION_SHORT` - короткая версия (3.0.0-a1b2c3d)
   - `VERSION_BASE` - базовая версия (3.0.0)
   - `VERSION_GIT_COMMIT` - короткий хеш коммита
   - `VERSION_GIT_COMMIT_FULL` - полный хеш коммита
   - `VERSION_GIT_BRANCH` - название ветки
   - `VERSION_BUILD_DATE` - дата сборки
   - `VERSION_BUILD_TIMESTAMP` - timestamp сборки
4. Передает версию в скрипт развертывания через переменные окружения

### 4. Отображение в основном скрипте

Скрипт `install-monitoring-stack.sh`:

1. Принимает версионные переменные из Jenkins:
   - `DEPLOY_VERSION` - версия развертывания
   - `DEPLOY_GIT_COMMIT` - git коммит
   - `DEPLOY_BUILD_DATE` - дата сборки

2. Отображает версию в заголовке при запуске:

```
================================================================
     MONITORING STACK AUTOMATION - DEPLOYMENT SCRIPT
================================================================
  Version:        3.0.0-a1b2c3d
  Git Commit:     a1b2c3d
  Build Date:     2026-01-23 14:30:00 UTC
  Script Start:   2026-01-23 14:35:00 UTC
================================================================
```

3. Сохраняет версию в state file (`/var/lib/monitoring_deployment_state`)

4. Отображает версию в финальном summary

---

## 🔄 Semantic Versioning

Проект следует [Semantic Versioning 2.0.0](https://semver.org/):

```
MAJOR.MINOR.PATCH

3.0.0
│ │ │
│ │ └─── PATCH: исправления ошибок, обновления документации
│ └───── MINOR: новые функции (обратная совместимость)
└─────── MAJOR: breaking changes
```

### Когда увеличивать версию

#### MAJOR (X.0.0)

Увеличивайте когда делаете **несовместимые изменения API**:
- Изменение параметров Jenkins pipeline (удаление обязательных параметров)
- Изменение структуры конфигурационных файлов
- Изменение имен wrapper'ов или их интерфейсов
- Изменение требований к sudoers

**Пример:** 2.8.0 → 3.0.0 (переименование всех файлов проекта)

#### MINOR (x.Y.0)

Увеличивайте когда добавляете **новую функциональность с обратной совместимостью**:
- Добавление новых опциональных параметров в pipeline
- Добавление новых wrapper'ов
- Добавление новых функций в скрипт развертывания
- Добавление поддержки новых сервисов

**Пример:** 2.7.0 → 2.8.0 (исправления bug'ов в Grafana API)

#### PATCH (x.y.Z)

Увеличивайте когда делаете **обратносовместимые исправления ошибок**:
- Исправление bug'ов
- Обновление документации
- Улучшение логирования
- Оптимизация производительности

**Пример:** 3.0.0 → 3.0.1 (исправление typo в документации)

---

## 📝 Процесс обновления версии

### Шаг 1: Определите тип изменений

Определите какой тип версии нужно обновить (MAJOR, MINOR, PATCH) согласно правилам выше.

### Шаг 2: Обновите файл VERSION

Откройте файл `VERSION` в корне проекта и обновите версию:

```bash
# Например, для обновления с 3.0.0 до 3.1.0
echo "3.1.0" > VERSION
```

### Шаг 3: Обновите CHANGELOG.md

Добавьте новую запись в `CHANGELOG.md`:

```markdown
## [3.1.0] - 2026-01-24 - 🚀 New Feature

### Added
- ✨ Добавлена поддержка новой функции X
- 📝 Обновлена документация

### Changed
- 🔧 Улучшена производительность Y

### Fixed
- 🐛 Исправлена ошибка в Z
```

### Шаг 4: Обновите версию в документации

Обновите версию в следующих файлах:
- `README.md` (badge и changelog)
- `AI_GUIDE.md` (Quick Overview и Key Statistics)
- `PROJECT_INFO.md` (Версионирование)

**Автоматический способ** (Linux/Mac):

```bash
# Скрипт для автоматического обновления версии
./tools/update-version-in-docs.sh 3.1.0
```

### Шаг 5: Commit и tag

```bash
# Commit изменений
git add VERSION CHANGELOG.md README.md AI_GUIDE.md PROJECT_INFO.md
git commit -m "chore: bump version to 3.1.0"

# Создайте git tag
git tag -a v3.1.0 -m "Release version 3.1.0"

# Push изменений и тега
git push origin master
git push origin v3.1.0
```

### Шаг 6: Проверьте в Jenkins

Запустите Jenkins pipeline и убедитесь, что новая версия отображается:

```
╔════════════════════════════════════════════════════════════════╗
║          MONITORING STACK AUTOMATION - VERSION INFO           ║
╠════════════════════════════════════════════════════════════════╣
║  Version:        3.1.0
║  Git Commit:     b2c3d4e (master)
║  Full Hash:      b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1
║  Build Date:     2026-01-24 10:15:30 UTC
║  Build ID:       20260124_101530
╚════════════════════════════════════════════════════════════════╝
```

---

## 🔍 Проверка версии

### В Jenkins Console

Версия отображается на первом этапе pipeline:

```
[Pipeline] stage (CI: Информация о версии проекта)
...
╔════════════════════════════════════════════════════════════════╗
║          MONITORING STACK AUTOMATION - VERSION INFO           ║
╠════════════════════════════════════════════════════════════════╣
║  Version:        3.0.0
║  Git Commit:     a1b2c3d (master)
...
```

### На целевом сервере

После развертывания проверьте state file:

```bash
cat /var/lib/monitoring_deployment_state
```

Вывод:
```
DEPLOYMENT_VERSION=3.0.0-a1b2c3d
DEPLOYMENT_GIT_COMMIT=a1b2c3d
DEPLOYMENT_BUILD_DATE=2026-01-23 14:30:00 UTC
INSTALL_DATE=20260123_143500
...
```

### Локально

Проверьте версию локально:

```bash
# Базовая версия
cat VERSION

# Полная информация
./tools/get-version.sh banner
```

---

## 🛠️ Troubleshooting

### Версия отображается как "unknown"

**Проблема:** В Jenkins или на сервере версия показывается как "unknown"

**Решение:**

1. Проверьте наличие файла `VERSION` в корне проекта
2. Убедитесь, что `tools/get-version.sh` исполняемый:
   ```bash
   chmod +x tools/get-version.sh
   ```
3. Проверьте что git репозиторий доступен в Jenkins workspace
4. Проверьте логи Jenkins для ошибок в стейдже "CI: Информация о версии проекта"

### Git информация отображается как "nogit"

**Проблема:** Git commit и branch показываются как "nogit" или "nobranch"

**Решение:**

1. Убедитесь что `.git` директория доступна в Jenkins workspace
2. Проверьте права доступа к git репозиторию
3. В Jenkins убедитесь что используется правильный SCM checkout

### Несовпадение версий в разных файлах

**Проблема:** Версия в `VERSION` не совпадает с версией в документации

**Решение:**

1. Обновите все файлы с версией согласно разделу "Процесс обновления версии"
2. Используйте автоматический скрипт для синхронизации версий
3. Добавьте в CI проверку согласованности версий

---

## 📊 История версий

Полная история версий проекта доступна в файле [CHANGELOG.md](CHANGELOG.md).

### Последние релизы

| Версия | Дата | Описание |
|--------|------|----------|
| 3.0.2 | 2026-01-23 | 🔄 Real-time Task Monitoring |
| 3.0.1 | 2026-01-23 | 🧹 Documentation Optimization |
| 3.0.0 | 2026-01-23 | 🎨 Professional Naming & Final Polish |
| 2.8.0 | 2026-01-20 | 🔧 Bug Fixes & Stability |
| 2.7.0 | 2026-01-15 | 🔒 Enhanced Security & Monitoring |
| 2.6.0 | 2026-01-08 | 👥 User Units & KAE Model |
| 2.5.0 | 2025-12-20 | 🔐 SHA256 Integrity Checks |

---

## 🔗 Связанные документы

- [README.md](README.md) - Основная документация проекта
- [CHANGELOG.md](CHANGELOG.md) - Полная история изменений
- [PROJECT_INFO.md](PROJECT_INFO.md) - Метаинформация проекта
- [AI_GUIDE.md](AI_GUIDE.md) - Руководство для AI ассистентов

---

## 📞 Вопросы и поддержка

Если у вас есть вопросы по версионированию:

1. Проверьте этот документ
2. Посмотрите примеры в CHANGELOG.md
3. Обратитесь к команде DevOps

---

<div align="center">

**Monitoring Stack Automation v3.0.2**

*Automatic version tracking for reliable deployments*

[Documentation](README.md) • [Changelog](CHANGELOG.md) • [Project Info](PROJECT_INFO.md)

</div>
